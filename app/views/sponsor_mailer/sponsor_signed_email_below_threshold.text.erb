<%= t "mail.greeting"%> <%= @petition.creator.name %>,

<%= t "mail.because.supported", sponsor: @sponsor.name, petition: @petition.action %>

<% if @sponsor_count == 1 %>
    <%= t "mail.sponsor.supporters.one", count: @sponsor_count, threshold: Site.threshold_for_moderation%>
<% else %>
    <%= t "mail.sponsor.supporters.other", count: @sponsor_count, threshold: Site.threshold_for_moderation%>
<% end %>

<%= t "mail.sponsor.moderation_info" %>
<%= help_url(anchor: 'standards') %>

<%= t "mail.thanks"%>
<%= t("petitions.emails.signoff_prefix") %>
<%= t("petitions.emails.signoff_suffix") %>
