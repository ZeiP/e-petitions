<h1 class="page-title">
  <% if @postcode.present? %>
    LOCALIZE_We couldn't find that postcode
  <% else %>
    LOCALIZE_Local to you
  <% end %>
</h1>

<% if @postcode.blank? %>
  <p>LOCALIZE_Find petitions about local issues</p>
<% else %>
  <p>LOCALIZE_We couldn't find the postcode <q><%= params[:postcode] %></q>. Try again?</p>
<% end %>

<%= render 'local_search' %>
